import "@/styles/common/spinner.scss";

const Spinner = () => {
  return (
    <div className="spinner" aria-label="Loading">
      {/* TODO:Add accessible description for screen readers */}
    </div>
  );
};

export default Spinner;
